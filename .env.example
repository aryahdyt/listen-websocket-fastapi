# Application
DEBUG=False

# Server
HOST=0.0.0.0
PORT=8000

# ClickHouse Configuration
CLICKHOUSE_HOST=192.168.7.22
CLICKHOUSE_PORT=9000
CLICKHOUSE_DATABASE=vessel_tracking
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# Redis Configuration
REDIS_HOST=192.168.2.68
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_TTL=3600  # Time to live in seconds
REDIS_PREFIX=projection_bpp_cache_

# WebSocket Configuration (Event-Driven Mode)
WEBSOCKET_URL=ws://202.10.32.106:1880/ws/projection_bpp
WEBSOCKET_RECONNECT_DELAY=5
WEBSOCKET_AUTO_START=True

# Polling System Configuration
# Enable/disable continuous polling mode
ENABLE_POLLING=True

# Polling interval in seconds (how often to fetch and match data)
POLL_INTERVAL_S=1.0

# Bootstrap period in minutes (fetch historical data on startup)
BOOTSTRAP_MINUTES=60

# Cache TTL in seconds (remove old entries)
CACHE_TTL_S=3600

# Site Location Configuration
# Center point for bbox calculation
SITE_LAT=-1.279656
SITE_LON=116.809655

# Filter radius in kilometers (bbox calculation)
FILTER_RADIUS_KM=60.0

# Projection System
# EPSG code for coordinate transformation
PROJECTION=EPSG:32650

# Matching Algorithm Parameters
# Maximum distance to consider for matching (meters)
GATING_DISTANCE_M=8000.0

# Maximum time difference for matching (seconds)
TIME_GATE_S=1800.0

# Minimum match score threshold (0.0 - 1.0)
MATCH_THRESHOLD=0.6

# Scoring Parameters (for matching algorithm)
# Position uncertainty (meters)
POS_SIGMA_M=500.0

# Speed uncertainty (m/s)
SPD_SIGMA_MS=3.0

# Heading uncertainty (degrees)
HDG_SIGMA_DEG=40.0

# Time uncertainty (seconds)
TIME_SIGMA_S=60.0
